# 核心 (回测 + 策略, 离线即可运行)
numpy>=1.24
pandas>=2.0
pyarrow>=14.0
scipy>=1.10
scikit-learn>=1.3

# 测试
pytest>=7.4

# === 实盘可选依赖 (live trading) ===
# 不装也能运行回测; 若启动 live runner 会做 lazy import 检查
python-dotenv>=1.0.0
websocket-client>=1.6.0
requests>=2.31.0

# Polymarket CLOB V2 真实下单 (仅 live 时需要)
py-clob-client-v2>=0.15.0
# Polymarket Builder Relayer (Proxy/Safe 自动赎回需要)
# PyPI 0.0.1 过旧且与 GitHub 同为 version=0.0.1，升级时必须 pip uninstall 后再装（见 deploy 脚本）
py-builder-relayer-client @ https://github.com/Polymarket/py-builder-relayer-client/archive/refs/heads/main.zip
py-builder-signing-sdk>=0.0.1

# Polygon 链上调用 (USDC 余额/授权查询)
web3>=6.10.0

# === WebUI 远程管理面板 (可选) ===
# 仅启动 `twinengines.cli webui` 时需要; 不影响策略本体
flask>=3.0.0
# psutil 可选: 装了 ⑬ 系统资源卡片才有 CPU/RAM 细粒度
psutil>=5.9.0
