# 生存模型训练方法改进方案

## 📋 目录
1. 背景：现有生存模型
2. 现有训练方式
3. 发现的问题
4. 新训练方式（核心改进）
5. 实施方案
6. 理论分析
7. 待验证的问题

---

## 1. 背景：现有生存模型

### 1.1 模型公式

生存模型用于预测价格反转的概率：

```python
# 瞬时风险率（hazard rate）
lambda(d, T) = lambda0 * (T/120)^gamma * exp(beta * d)

# 累积风险（cumulative hazard）
H(d, T) = lambda0 * exp(beta * d) * T^(gamma+1) / (120^gamma * (gamma+1))

# 反转概率
P(reversal | d, T) = 1 - exp(-H(d, T))
```

**参数说明**：
- `d`：当前价格相对基准价格的绝对偏离百分比（取正值）
- `T`：剩余时间（秒）
- `lambda0`：基础风险率
- `gamma`：时间衰减指数
- `beta`：价格偏离敏感度

### 1.2 模型含义

这是一个**非齐次泊松过程**模型：
- 价格偏离越大（d越大），反转概率越高
- 剩余时间越长（T越大），反转概率越高
- 通过参数lambda0, gamma, beta控制这些关系的强度

---

## 2. 现有训练方式

### 2.1 训练目标

**最大化似然函数（Maximum Likelihood Estimation）**：

```python
# 对每个窗口，计算累积风险
H_i = sum_k lambda(d_k, T_k) * dt_k  # 秒级路径积分

# 反转概率
P_i = 1 - exp(-H_i)

# 似然函数
log_likelihood = sum_i [
    event_i * log(P_i) + (1 - event_i) * log(1 - P_i)
]

# 训练目标：最大化 log_likelihood
# 即：找到参数 (lambda0, gamma, beta) 使得预测概率最接近实际结果
```

**训练数据**：
- 输入：历史K线数据
- 特征：每秒的 (d, T)
- 标签：event = 1 if 价格反转 else 0

### 2.2 决策逻辑

训练完成后，实盘决策：

```python
# 计算反转概率
p_rev = survival_model.predict(d, T)

# 决策规则
if p_rev > 0.5:  # 必须大于50%
    buy_reversal()  # 只买反转方向
```

**限制**：
1. 只考虑预测准确率，不考虑盘口价格
2. 必须 p_rev > 0.5 才下单
3. 只做反转方向，不做趋势方向

---

## 3. 发现的问题

### 3.1 不考虑实时盘口

**问题**：
```python
# 场景1：预测准确，但盘口太差
p_rev = 0.7  # 70%会反转
best_ask = 0.9  # 但盘口价格很高

# 期望EV
ev = 0.7 * (1 - 0.9) - 0.3 * 0.9 = 0.07 - 0.27 = -0.20  # 负EV！

# 但现有模型会下单（因为p_rev > 0.5）
```

**结果**：虽然预测准确率高，但实际亏钱。

### 3.2 限制太多，错过机会

**问题1：小概率但盘口好**
```python
p_rev = 0.3  # 只有30%会反转
best_ask = 0.1  # 但盘口极好

# 期望EV
ev = 0.3 * (1 - 0.1) - 0.7 * 0.1 = 0.27 - 0.07 = 0.20  # 正EV！

# 但现有模型不会下单（因为p_rev < 0.5）
```

**问题2：趋势方向盘口好**
```python
p_rev = 0.2  # 只有20%会反转，80%会延续趋势
best_ask_trend = 0.15  # 趋势方向盘口很好

# 期望EV（做趋势）
ev = 0.8 * (1 - 0.15) - 0.2 * 0.15 = 0.68 - 0.03 = 0.65  # 巨大正EV！

# 但现有模型禁止做趋势
```

### 3.3 训练目标不对齐

**核心问题**：
- 训练目标：最大化预测准确率
- 实际目标：最大化收益（EV）

这两个目标不一致！

---

## 4. 新训练方式（核心改进）

### 4.1 核心思想

**保持模型公式不变，改变训练目标**：

```python
# 公式不变
P(reversal | d, T) = 1 - exp(-H(d, T))
H(d, T) = lambda0 * exp(beta * d) * T^(gamma+1) / (120^gamma * (gamma+1))

# 但训练目标改为：最大化实际EV
```

### 4.2 新的训练目标

**直接优化实际收益**：

```python
def ev_loss(params, data):
    """
    EV损失函数
    
    params: [lambda0, gamma, beta]
    data: 训练数据（包含盘口和结算结果）
    """
    lambda0, gamma, beta = params
    
    total_ev = 0
    
    for window in data:
        d = window['d_abs_pct']
        T = window['t_remaining']
        best_ask_reversal = window['best_ask_reversal']  # 反转方向盘口
        best_ask_trend = window['best_ask_trend']  # 趋势方向盘口
        actual_reversal = window['actual_reversal']  # 实际结果
        
        # 1. 用生存模型预测反转概率
        p_rev = survival_formula(d, T, lambda0, gamma, beta)
        
        # 2. 计算两个方向的期望EV
        ev_reversal = p_rev * (1 - best_ask_reversal) - (1 - p_rev) * best_ask_reversal
        ev_trend = (1 - p_rev) * (1 - best_ask_trend) - p_rev * best_ask_trend
        
        # 3. 决策：选EV更高的方向
        if ev_reversal > ev_trend and ev_reversal > 0:
            decision = 'reversal'
            price = best_ask_reversal
        elif ev_trend > 0:
            decision = 'trend'
            price = best_ask_trend
        else:
            decision = 'none'
            price = 0
        
        # 4. 计算实际EV（以结算结果为准）
        if decision == 'reversal':
            if actual_reversal:
                actual_ev = (1 - price)  # 赢了
            else:
                actual_ev = -price  # 输了
        elif decision == 'trend':
            if not actual_reversal:
                actual_ev = (1 - price)  # 赢了
            else:
                actual_ev = -price  # 输了
        else:
            actual_ev = 0  # 没下单
        
        total_ev += actual_ev
    
    return -total_ev  # 最小化负EV = 最大化EV


# 训练
from scipy.optimize import minimize

result = minimize(
    ev_loss,
    x0=[0.1, 1.0, 0.0],  # 初始参数
    args=(training_data,),
    method='L-BFGS-B',
    bounds=[
        (1e-6, 20.0),   # lambda0
        (0.1, 4.0),     # gamma
        (-8.0, 8.0),    # beta
    ]
)

lambda0, gamma, beta = result.x
```

### 4.3 关键改进点

**改进1：考虑实时盘口**
- 训练时使用实际盘口价格
- 决策时综合考虑概率和价格

**改进2：不限定方向**
- 可以做反转（买YES）
- 可以做趋势（买NO）
- 选EV更高的方向

**改进3：不限定概率阈值**
- p_rev = 0.3 也可以下单（如果盘口好）
- p_rev = 0.7 也可以不下单（如果盘口差）

**改进4：直接优化目标**
- 训练目标 = 实际目标 = 最大化EV
- 完美对齐

### 4.4 新的决策逻辑

```python
# 实盘决策
p_rev = survival_model.predict(d, T)

# 计算两个方向的EV
ev_reversal = p_rev * (1 - best_ask_reversal) - (1 - p_rev) * best_ask_reversal
ev_trend = (1 - p_rev) * (1 - best_ask_trend) - p_rev * best_ask_trend

# 决策
if ev_reversal > ev_trend and ev_reversal > 0:
    buy_reversal()
elif ev_trend > 0:
    buy_trend()
else:
    pass  # 不下单
```

---

## 5. 实施方案

### 5.1 数据需求

**训练数据必须包含**：

```python
{
    # 特征
    "d_abs_pct": 0.5,           # 价格偏离
    "t_remaining_sec": 120,     # 剩余时间
    
    # 盘口（关键！）
    "best_ask_reversal": 0.35,  # 反转方向的盘口价格
    "best_ask_trend": 0.68,     # 趋势方向的盘口价格
    
    # 结算结果（关键！）
    "actual_reversal": True,    # 最终是否反转
}
```

**数据来源**：
- 实时采集：每个窗口记录信号时的盘口
- 结算回填：窗口结束后查询Binance K线，判断是否反转

### 5.2 滚动训练方案

**每10小时训练一次**：

```
时间轴：
[0-10h]  → 训练模型A → 验证[10-20h] → 记录结果
[0-20h]  → 训练模型B → 验证[20-30h] → 记录结果
[0-30h]  → 训练模型C → 验证[30-40h] → 记录结果
...
```

**优势**：
1. 数据越来越多（持续学习）
2. 前向验证（避免未来信息泄露）
3. 自动追踪改进
4. 适应市场变化

### 5.3 实施步骤

**已完成**：
1. ✅ 修改采集器，记录盘口信息
2. ✅ 添加结算回填功能
3. ✅ 部署到VPS运行

**待完成**：
1. ⏳ 等待6-12小时积累数据（80-150条）
2. ⏳ 训练EV优化模型
3. ⏳ 实盘验证1小时
4. ⏳ 对比效果

---

## 6. 理论分析

### 6.1 为什么这个方案理论上可行？

**原因1：目标对齐**
```
训练目标 = 实际目标 = 最大化EV
```
不再有"预测准确但亏钱"的问题。

**原因2：信息更完整**
```
旧方法：只用 (d, T) → P(reversal)
新方法：用 (d, T, best_ask) → 决策
```
考虑了盘口信息，更贴近实际交易。

**原因3：不限定方向**
```
旧方法：只做反转
新方法：反转和趋势都考虑
```
理论上一定存在正期望解。

**原因4：数学上合理**
```
生存模型给出 P(reversal)
结合盘口计算 EV
选择 max(EV_reversal, EV_trend)
```
这是标准的期望值决策理论。

### 6.2 理论上的优势

**优势1：更多交易机会**
- 不限定 p > 0.5
- 不限定只做反转
- 只要 EV > 0 就可以交易

**优势2：更高的总EV**
- 直接优化目标
- 避免"准确但不赚钱"
- 避免"错过高EV机会"

**优势3：自动适应市场**
- 滚动训练
- 持续学习
- 参数自动调整

### 6.3 与现有模型的关系

**不是替代，是改进**：
```
公式：完全相同
参数：重新训练
目标：从准确率 → EV
```

**可以对比**：
- 旧参数：用MLE训练
- 新参数：用EV训练
- 对比验证集上的表现

---

## 7. 待验证的问题

### 7.1 数据量问题

**问题**：
- 10小时 = 120个窗口
- 但不是每个窗口都有信号
- 实际可能只有20-50条数据

**影响**：
- 数据少，训练效果可能不好
- 可能过拟合

**解决**：
- 初期用24小时数据（~100条）
- 稳定后改为10小时
- 或者用更长时间（7天）

### 7.2 过拟合风险

**问题**：
- 直接优化训练集EV
- 可能过拟合到训练集
- 验证集上表现差

**解决**：
- 前向验证（时间序列切分）
- 正则化（L2惩罚）
- 简单模型（保持公式不变）

### 7.3 参数可能学偏

**问题**：
- 如果训练数据中，趋势方向盘口总是更好
- 参数可能学到"总是预测低概率"
- 这样 ev_trend 总是更高

**影响**：
- 失去"预测反转"的能力
- 变成"总是做趋势"

**解决**：
- 加入约束（保持一定的预测准确率）
- 多目标优化（EV + 准确率）
- 监控参数变化

### 7.4 盘口数据质量

**问题**：
- 盘口可能瞬间变化
- 记录的盘口 ≠ 实际成交价格
- 可能有滑点

**影响**：
- 训练数据不准确
- 实际EV低于预期

**解决**：
- 记录盘口时间戳
- 考虑滑点（best_ask + slippage）
- 实盘验证

### 7.5 两个方向的盘口

**问题**：
- 理论上 ask_reversal + ask_trend ≈ 1
- 如果市场有效，两个方向EV都应该接近0
- 正EV机会可能很少

**影响**：
- 可能很少下单
- 或者数据中的"正EV"是噪音

**解决**：
- 实际验证
- 如果确实很少机会，说明市场有效
- 需要更精细的模型

---

## 8. 总结

### 8.1 核心改进

```
旧方法：
  训练目标 = 最大化预测准确率
  决策逻辑 = if p_rev > 0.5: buy_reversal()
  
新方法：
  训练目标 = 最大化实际EV
  决策逻辑 = if max(ev_reversal, ev_trend) > 0: buy_best_direction()
```

### 8.2 理论可行性

**✅ 理论上完全可行**：
1. 目标对齐（训练=实际）
2. 信息完整（考虑盘口）
3. 不限定方向（更多机会）
4. 数学合理（期望值决策）

**⚠️ 但需要注意**：
1. 数据量要足够
2. 防止过拟合
3. 参数可能学偏
4. 盘口数据质量
5. 实际验证

### 8.3 下一步

1. **等待数据**：6-12小时，积累80-150条
2. **训练模型**：用EV目标训练
3. **验证效果**：前向验证，对比旧模型
4. **实盘测试**：1小时实盘，对比收益
5. **持续改进**：滚动训练，自动优化

---

## 9. 请DeepSeek分析

### 9.1 核心问题

1. **理论可行性**：这个方案在理论上是否成立？
2. **数学正确性**：训练目标的设计是否合理？
3. **潜在问题**：有哪些我们没考虑到的风险？
4. **改进建议**：如何优化这个方案？

### 9.2 具体疑问

1. **参数学偏问题**：如何防止参数学到"总是预测低概率"？
2. **数据量问题**：10小时120个窗口够不够？
3. **过拟合问题**：如何在小数据集上避免过拟合？
4. **多目标优化**：是否应该同时优化EV和准确率？
5. **正则化方法**：应该用什么正则化？

### 9.3 期望反馈

- 理论分析
- 潜在风险
- 改进建议
- 类似方法的参考文献

---

**文档完成时间**：2026-05-03 20:30
**当前状态**：采集器已部署，等待数据积累
**预计训练时间**：2026-05-04 08:00（12小时后）
